# Benchmark methodology

Each benchmark documents warmup count, measured repetitions, outlier policy,
clock source, raw-sample retention, and the distinction between measured,
calculated, and estimated values.

